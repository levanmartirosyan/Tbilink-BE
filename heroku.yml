build:
  docker:
    web: Dockerfile
  
  config:
    ASPNETCORE_ENVIRONMENT: Production
    
release:
  command:
    - dotnet Tbilink-BE.WebApi.dll -- migrate

run:
  web: dotnet Tbilink-BE.WebApi.dll